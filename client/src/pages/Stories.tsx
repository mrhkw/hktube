import{useEffect,useState}from"react";import{HkTubeShell}from"@/components/HkTubeShell";import{supabase}from"@/lib/supabase";import{Button}from"@/components/ui/button";import{Textarea}from"@/components/ui/textarea";import{Input}from"@/components/ui/input";import{toast}from"sonner";import{Archive,Eye,ImagePlus,Plus,Trash2}from"lucide-react";
type Story={id:string;user_id:string;media_path:string|null;text_content:string|null;media_type:string;visibility:string;expires_at:string;created_at:string;archived_at:string|null};
function url(s:Story){if(!s.media_path)return null;return s.media_path.startsWith("http")?s.media_path:supabase.storage.from("stories").getPublicUrl(s.media_path).data.publicUrl}
function Media({story,viewer=false}:{story:Story;viewer?:boolean}){const u=url(story);if(!u)return <div className={viewer?"min-h-64 p-8 text-xl font-semibold text-white":"p-3 text-sm font-semibold text-white"}>{story.text_content}</div>;if(story.media_type.startsWith("video/"))return <video src={u} controls={viewer} autoPlay={viewer} playsInline className={viewer?"max-h-[70vh] w-full object-contain":"size-full object-cover"}/>;return <img src={u} alt="" className={viewer?"max-h-[70vh] w-full object-contain":"size-full object-cover"}/>}
export default function Stories(){const[user,setUser]=useState<any>(null),[active,setActive]=useState<Story|null>(null),[stories,setStories]=useState<Story[]>([]),[mine,setMine]=useState<Story[]>([]),[highlights,setHighlights]=useState<any[]>([]),[text,setText]=useState(""),[visibility,setVisibility]=useState("followers"),[file,setFile]=useState<File|null>(null),[views,setViews]=useState(0),[name,setName]=useState(""),[selectedHighlight,setSelectedHighlight]=useState<string|null>(null),[busy,setBusy]=useState(false);
async function load(){const u=(await supabase.auth.getUser()).data.user;if(!u){setUser(null);return}setUser(u);const now=new Date().toISOString();const[a,b,c]=await Promise.all([supabase.from("stories").select("*").gt("expires_at",now).order("created_at",{ascending:false}).limit(100),supabase.from("stories").select("*").eq("user_id",u.id).order("created_at",{ascending:false}).limit(200),supabase.from("story_highlights").select("*").eq("owner_id",u.id).order("created_at",{ascending:false})]);setStories(a.data||[]);setMine(b.data||[]);setHighlights(c.data||[])}
async function publish(){if(!user||(!text.trim()&&!file)||busy)return;setBusy(true);try{let path:null|string=null;if(file){if(file.size>20*1024*1024)throw Error("Story media must be 20 MB or smaller.");if(!file.type.startsWith("image/")&&!file.type.startsWith("video/"))throw Error("Choose an image or video.");path=user.id+"/"+crypto.randomUUID()+"-"+file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const up=await supabase.storage.from("stories").upload(path,file,{contentType:file.type,upsert:false});if(up.error)throw up.error}const r=await supabase.from("stories").insert({user_id:user.id,media_path:path,text_content:text.trim()||null,media_type:file?.type||"text",visibility,reply_allowed:true});if(r.error)throw r.error;setText("");setFile(null);toast.success("Story published for 24 hours.");await load()}catch(e){toast.error(e instanceof Error?e.message:"Could not publish story.")}finally{setBusy(false)}}
async function view(s:Story){setActive(s);if(user&&s.user_id!==user.id)await supabase.from("story_views").insert({story_id:s.id,viewer_id:user.id}).then(()=>undefined).catch(()=>undefined);const r=await supabase.from("story_views").select("viewer_id",{count:"exact",head:true}).eq("story_id",s.id);setViews(r.count||0)}
async function createHighlight(){if(!user||!name.trim())return;const r=await supabase.from("story_highlights").insert({owner_id:user.id,title:name.trim()}).select().single();if(r.error)toast.error(r.error.message);else{setName("");setSelectedHighlight(r.data.id);setHighlights(v=>[r.data,...v])}}
async function add(s:Story){if(!selectedHighlight)return;const r=await supabase.from("story_highlight_items").upsert({highlight_id:selectedHighlight,story_id:s.id,position:Date.now()});if(r.error)toast.error(r.error.message);else toast.success("Added to highlight.")}
async function archive(s:Story){const r=await supabase.from("stories").update({archived_at:new Date().toISOString()}).eq("id",s.id).eq("user_id",user.id);if(r.error)toast.error(r.error.message);else await load()}
useEffect(()=>{void load()},[]);
if(!user)return <HkTubeShell title="Stories"><div className="p-10 text-center text-slate-400">Sign in to use Stories.</div></HkTubeShell>;
return <HkTubeShell title="Stories"><main className="mx-auto max-w-6xl p-4"><section className="rounded-3xl border border-white/10 bg-white/[.03] p-5"><h1 className="text-2xl font-black text-white">Stories</h1><p className="mt-1 text-sm text-slate-500">Photo, video and text stories expire after 24 hours.</p><div className="mt-4 grid gap-3 md:grid-cols-[1fr_auto_auto]"><Textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Write a story..." className="min-h-24 border-white/10 bg-white/[.04] text-white"/><label className="flex cursor-pointer items-center justify-center rounded-xl border border-dashed border-white/15 px-4 text-sm text-slate-300"><ImagePlus className="mr-2 size-4"/>{file?file.name:"Photo / video"}<Input type="file" accept="image/*,video/mp4,video/webm" className="hidden" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><div className="flex gap-2"><select value={visibility} onChange={e=>setVisibility(e.target.value)} className="rounded-xl border border-white/10 bg-[#151a25] px-3 text-white"><option value="followers">Followers</option><option value="public">Public</option><option value="close_friends">Close friends</option></select><Button onClick={()=>void publish()} disabled={busy||(!text.trim()&&!file)}>{busy?"Publishing…":"Publish"}</Button></div></div></section><section className="mt-6"><h2 className="mb-3 font-bold text-white">Active stories</h2><div className="flex gap-3 overflow-x-auto pb-2">{stories.map(s=><button key={s.id} onClick={()=>void view(s)} className="relative h-44 w-28 shrink-0 overflow-hidden rounded-2xl border border-white/10 bg-violet-500/10 text-left"><Media story={s}/></button>)}</div></section><section className="mt-8 rounded-2xl border border-white/10 p-4"><div className="flex flex-wrap items-center gap-2"><Archive className="size-4 text-violet-300"/><h2 className="font-bold text-white">Archive & highlights</h2><span className="text-xs text-slate-500">{mine.length} stories</span><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Highlight name" className="ml-auto max-w-52"/><Button size="sm" onClick={()=>void createHighlight()}><Plus className="mr-1 size-4"/>Create</Button></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{mine.map(s=><div key={s.id} className="flex items-center gap-3 rounded-xl border border-white/8 p-3"><div className="size-14 overflow-hidden rounded-lg bg-white/5"><Media story={s}/></div><div className="min-w-0 flex-1"><p className="truncate text-sm text-white">{s.text_content||s.media_type}</p><p className="text-xs text-slate-500">{s.archived_at?"Archived":"Active"}</p></div><Button variant="ghost" size="icon" onClick={()=>void archive(s)} aria-label="Archive story"><Trash2 className="size-4"/></Button>{selectedHighlight&&<Button size="sm" variant="outline" onClick={()=>void add(s)}>Add</Button>}</div>)}</div><div className="mt-4 flex flex-wrap gap-2">{highlights.map(h=><button key={h.id} onClick={()=>setSelectedHighlight(h.id)} className={"rounded-full border px-3 py-1.5 text-xs "+(selectedHighlight===h.id?"border-violet-400 bg-violet-500/20 text-white":"border-white/10 text-slate-300")}>{h.title}</button>)}</div></section>{active&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/80 p-4"><div className="relative max-h-[90vh] w-full max-w-md overflow-hidden rounded-3xl border border-white/10 bg-[#0c1019]"><button onClick={()=>setActive(null)} className="absolute right-3 top-3 z-10 rounded-full bg-black/60 px-3 py-1 text-white" aria-label="Close story">×</button><Media story={active} viewer/><div className="flex items-center gap-2 border-t border-white/10 p-4 text-sm text-slate-300"><Eye className="size-4"/>{views} views<span className="ml-auto text-xs text-slate-500">{new Date(active.expires_at).toLocaleString()}</span></div></div></div>}</main></HkTubeShell>}