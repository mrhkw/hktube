import { useEffect, useState } from "react";
import { useLocation } from "wouter";
import { HkTubeShell } from "@/components/HkTubeShell";
import { SupabaseVideoCard } from "@/components/SupabaseVideoCard";
import { searchPublicVideos } from "@/lib/supabaseEngagement";
import { Loader2, Search } from "lucide-react";

export default function SupabaseSearch(){const [location]=useLocation();const query=new URLSearchParams(location.split("?")[1]||"").get("q")?.trim()||"";const [results,setResults]=useState<any[]>([]);const [loading,setLoading]=useState(false);useEffect(()=>{if(!query){setResults([]);return;}let active=true;setLoading(true);void searchPublicVideos(query,48).then(rows=>{if(active)setResults(rows);}).catch(()=>{if(active)setResults([]);}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[query]);return <HkTubeShell title="Search"><main className="mx-auto w-full max-w-[1480px] px-4 pb-16 sm:px-8 lg:px-10">{query?<><div className="mb-7 flex items-center gap-3"><Search className="size-5 text-violet-300"/><div><p className="text-xs uppercase tracking-[.18em] text-slate-500">Search results</p><h1 className="text-2xl font-black text-white">Results for “{query}”</h1></div></div>{loading?<div className="grid min-h-[35vh] place-items-center"><Loader2 className="size-8 animate-spin"/></div>:results.length?<div className="grid gap-x-5 gap-y-8 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{results.map(v=><SupabaseVideoCard key={v.id} video={{id:String(v.id),title:v.title,thumbnailUrl:v.thumbnail_path&&v.thumbnail_path.startsWith("http")?v.thumbnail_path:null,durationSeconds:v.duration_seconds,views:v.views,publishedAt:v.published_at,isShort:Array.isArray(v.tags)&&v.tags.includes("shorts")}}/>)}</div>:<div className="rounded-3xl border border-white/10 bg-white/[.03] p-12 text-center text-slate-400">No public videos matched your search.</div>}</>:<div className="rounded-3xl border border-white/10 bg-white/[.03] p-12 text-center text-slate-400">Type something in the search box to find videos.</div>}</main></HkTubeShell>}
